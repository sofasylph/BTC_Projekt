#ifndef SORTING_HPP
#define SORTING_HPP

// Simple mergesort or quicksort for an array of floats

void mergeSort(float* arr, int left, int right);
void quickSort(float* arr, int left, int right);

#endif // SORTING_HPP